{
  "name": "Crossword",
  "install": "npm ci",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Runs the Express API (port 3001) and the Vite client dev server (port 5173) together via concurrently. Open the client on port 5173; it proxies /api requests to the server."
    }
  ],
  "ports": [
    {
      "name": "client",
      "port": 5173
    },
    {
      "name": "server",
      "port": 3001
    }
  ]
}
